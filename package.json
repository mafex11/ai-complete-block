{
  "name": "ai-blocker",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome MV3 extension that blocks AI-related content.",
  "scripts": {
    "build": "node scripts/build-config.js",
    "watch": "nodemon -w .env -x \"npm run build\"",
    "zip": "npm run build && cd extension && powershell -Command Compress-Archive -Path * -DestinationPath ../dist/ai-blocker.zip",
    "prepare-dist": "node -e \"require('fs').mkdirSync('dist',{recursive:true})\""
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}



